apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    addonmanager.kubernetes.io/mode: "Reconcile"
  name: coredns
  namespace: kube-system
data:
  Corefile: |
  <%- scope['kubernetes::coredns_config'].split(/\n+/).each do |str| -%>
    <%= str %>
  <%- end -%>
